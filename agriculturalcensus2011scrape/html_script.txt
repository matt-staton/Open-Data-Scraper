from bs4 import BeautifulSoup
import pandas as pd
from pandas import DataFrame, Series
soup=BeautifulSoup(html) #change to URL

tag_body = soup.body
tag_table =tag_body.find_all("table")
tag_data = tag_table[3]
#tag_tr = tag_data.find_all("tr")
data_all=pd.DataFrame(columns=[range(1,16)], index = range(0,len(tag_tr)-3))
tag_tr = tag_data.find_all("tr") # finds tr tag

for j in range(3, len(tag_tr)):
    samplist =[]
    tag_sample = tag_tr[j] # starts reading from 4th 'tr' tag 
    list_td = list(tag_sample.find_all('td')) #result set
    a = tag_sample.find_all('td') # tag
    
    for i in range(1, len(list_td)):
        samplist.append(a[i].text)
    print samplist
    